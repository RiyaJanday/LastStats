// Deprecated: the logo is now imported directly as a static asset from
// ./logo.png (see LandingPage.jsx), which lets Vite optimize/hash it
// properly instead of shipping a compressed base64 copy in the JS bundle.
// This file is intentionally left empty and can be deleted.
